{
  "name": "wore-house-elt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "start": "npm --prefix frontend run preview -- --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "dependencies": {
    "vite": "^5.1.3"
  }
}
